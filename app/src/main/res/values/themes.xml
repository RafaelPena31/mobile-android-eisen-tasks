<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- App Theme -->
    <style name="AppNavigationTheme" parent="@style/Theme.Design.Light.NoActionBar">
        <item name="android:windowAnimationStyle">@style/WindowAnimationStyle</item>
    </style>

    <style name="WindowAnimationStyle">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!-- Size Theme Config -->
    <style name="DefaultSizeConfig">
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_width">0dp</item>
    </style>

    <!-- Button Themes -->
    <style name="DefaultButton" parent="DefaultSizeConfig">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="MainButton" parent="DefaultButton">
        <item name="android:background">@drawable/ic_round_view</item>
        <item name="backgroundTint">@color/yellow</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="DeleteButton" parent="DefaultButton">
        <item name="android:background">@drawable/ic_round_view</item>
        <item name="backgroundTint">@color/red</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="SecondaryButton" parent="DefaultButton">
        <item name="android:background">@drawable/ic_round_border_view</item>
        <item name="android:textColor">@color/text</item>
    </style>

    <!-- Input Themes -->
    <style name="MainInput" parent="DefaultSizeConfig">
        <item name="android:background">@drawable/ic_round_border_view</item>
        <item name="android:textColor">@color/grey_input</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:textSize">12sp</item>
    </style>
</resources>